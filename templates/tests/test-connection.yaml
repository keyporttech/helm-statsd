apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "statsd.fullname" . }}-test-connection"
  labels:
    {{- include "statsd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "statsd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
